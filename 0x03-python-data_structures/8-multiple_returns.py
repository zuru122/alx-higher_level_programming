#!/usr/bin/python3
# Author: Sunday, Justice Gabriel

def multiple_returns(sentence):
    if len(sentence) == '':
        first == None
    else:
        first  = sentence[0]
    length =  len(sentence)
    return  length, first
